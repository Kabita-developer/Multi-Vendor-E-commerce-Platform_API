const express = require('express');

const router = express.Router();

// This file is kept for potential future admin-specific category routes
// All category CRUD operations are now handled in categoryRoutes.js

module.exports = router;


const express = require('express');

const router = express.Router();

// This file is kept for potential future admin-specific brand routes
// All brand CRUD operations are now handled in brandRoutes.js

module.exports = router;

